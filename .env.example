# Canonical absolute site URL (no trailing slash).
# Used for metadata, canonical tags, Open Graph, sitemap, and robots.
# Set this to your production URL after deploying (Vercel default or custom domain).
NEXT_PUBLIC_SITE_URL=https://your-domain.com
